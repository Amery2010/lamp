/**
 * 判断是否为 IOS 设备（iPhone、Ipad、Ipod等）
 * @returns 返回 boolean
 * @example
 * ```
 * isIOS() => true
 * ```
 * @author Amery
 * @version v0.1.0
 * @since v0.1.0
 */
declare function isIOS(): boolean;
export default isIOS;
